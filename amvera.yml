meta:
  environment: golang
  toolchain:
    name: go
    version: 1.22
build:
  image: golang:1.22
run:
  image: alpine:latest  # Используем легковесный образ для запуска
  command: ./bin/bot     # Команда для запуска вашего приложения
  persistenceMount: /data
  containerPort: 80