meta:
  environment: golang
  toolchain:
    name: go
    version: 1.22
build:
  image: golang:1.22
run:
  image: alpine:latest
  workingDir: /anon-chat-bot
  persistenceMount: /data
  containerPort: 80